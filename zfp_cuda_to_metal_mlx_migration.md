# ZFP CUDA -> Metal / MLX Migration Plan (Implementation-Ready)

**Repository baseline:** current `develop` layout in this repo  
**Primary source to port:** `src/cuda_zfp/cuZFP.cu` + `src/cuda_zfp/*.cuh`  
**Target platforms:** Apple Silicon first (M1+), Intel Mac + AMD optional/secondary  
**Target runtimes:** Metal backend in core library; MLX wrapper as optional addon  
**Estimated effort:** ~8 weeks (Metal), ~11 weeks (Metal + MLX)

---

## Table of contents

1. [Goals and non-goals](#1-goals-and-non-goals)
2. [Current integration reality (important)](#2-current-integration-reality-important)
3. [Target architecture](#3-target-architecture)
4. [Phase 0 - Design freeze and acceptance criteria](#4-phase-0---design-freeze-and-acceptance-criteria)
5. [Phase 1 - Backend abstraction without API breakage](#5-phase-1---backend-abstraction-without-api-breakage)
6. [Phase 2 - Metal host runtime integration](#6-phase-2---metal-host-runtime-integration)
7. [Phase 3 - Kernel port CUDA -> MSL](#7-phase-3---kernel-port-cuda---msl)
8. [Phase 4 - Validation and performance tuning](#8-phase-4---validation-and-performance-tuning)
9. [Phase 5 - MLX integration (optional)](#9-phase-5---mlx-integration-optional)
10. [Risks and mitigations](#10-risks-and-mitigations)
11. [Proposed file layout](#11-proposed-file-layout)
12. [Timeline and milestones](#12-timeline-and-milestones)

---

## 1. Goals and non-goals

### Goals

- Add a Metal GPU backend for fixed-rate compression/decompression paths currently served by CUDA.
- Keep public C API source compatibility (`zfp.h` usage patterns unchanged for existing clients).
- Preserve existing dispatch behavior in `zfp_compress` / `zfp_decompress` tables (`src/zfp.c`).
- Maintain correctness first (byte-identity where expected), then optimize.

### Non-goals (initial release)

- No attempt to support every historical Mac GPU generation in v1.
- No immediate expansion to 4D GPU kernels (current CUDA path already omits 4D GPU entries).
- No redesign of core bitstream format or compression semantics.

---

## 2. Current integration reality (important)

The repo does not call a generic `gpu_compress()` today. Instead:

- `src/zfp.c` uses function tables keyed by execution policy + strided + dims + type.
- CUDA entries are generated by template includes and eventually call:
  - `cuda_compress(stream, field)`
  - `cuda_decompress(stream, field)`
- Those come from `src/cuda_zfp/cuZFP.cu` via `src/template/cudacompress.c` and `src/template/cudadecompress.c`.

Implication for migration:

- We should **not** start by replacing all dispatch machinery.
- The fastest low-risk path is introducing a backend-internal abstraction while preserving the current `compress_*_cuda_*` and `decompress_*_cuda_*` call sites.

---

## 3. Target architecture

Keep `zfp` external behavior unchanged, but refactor backend internals:

```text
zfp public API (zfp.h) - unchanged
        |
        v
src/zfp.c function tables (unchanged shape)
        |
        v
compress_*_cuda_* / decompress_*_cuda_* wrappers
        |
        v
gpu backend bridge (new internal abstraction)
   |- cuda backend (existing path, refactored)
   |- metal backend (new)

optional:
MLX adapter reusing Metal kernels
```

Key principle: **internal abstraction, not public API churn**.

---

## 4. Phase 0 - Design freeze and acceptance criteria

**Duration:** 2-3 days

Lock the following before coding:

- Backend selection strategy:
  - Option A (recommended): keep using `zfp_exec_cuda` as the "GPU" policy and bind it to CUDA or Metal at build-time.
  - Option B: add new `zfp_exec_metal` enum value (higher compatibility risk; avoid in first pass).
- Build flags:
  - `ZFP_WITH_CUDA` (existing)
  - `ZFP_WITH_METAL` (new)
  - mutually exclusive in first implementation.
- Hard requirements:
  - Metal backend requires `ZFP_BIT_STREAM_WORD_SIZE=64`.
  - Unsupported configurations fail at configure time.

Acceptance criteria document should define:

- exact correctness tests,
- supported dimensions/types,
- fallback behavior,
- performance baseline metrics.

---

## 5. Phase 1 - Backend abstraction without API breakage

**Duration:** ~1 week

### 5.1 Add internal backend header

Create internal-only interface (not installed public header):

- `src/gpu_backend.h`

Example:

```c
// internal only
size_t zfp_gpu_compress(zfp_stream* stream, const zfp_field* field);
void   zfp_gpu_decompress(zfp_stream* stream, zfp_field* field);
```

### 5.2 Preserve current template wrappers

Update `src/template/cudacompress.c` / `src/template/cudadecompress.c` to call `zfp_gpu_compress` / `zfp_gpu_decompress` instead of directly calling `cuda_*` functions.

This keeps `src/zfp.c` tables unchanged and minimizes regression surface.

### 5.3 Extract shared host helpers from `cuZFP.cu`

Move pure logic to shared C++ files:

- contiguity checks (`is_contigous1d/2d/3d`, naming preserved initially),
- pointer offset helper (`offset_void`),
- default stride derivation.

Place in:

- `src/gpu_common.h`
- `src/gpu_common.cpp`

### 5.4 Isolate CUDA-specific runtime code

Refactor CUDA-specific pieces into dedicated CUDA TU(s):

- allocation/copies (`cudaMalloc`, `cudaMemcpy`, `cudaFree`)
- device pointer detection (`cuPointerGetAttributes` via helper)
- kernel launch wrappers.

This produces a cleaner baseline to mirror in Metal.

---

## 6. Phase 2 - Metal host runtime integration

**Duration:** ~1.5 weeks

### 6.1 Add Metal backend module

Create:

- `src/metal_zfp/metal_backend.mm`
- `src/metal_zfp/metal_backend.h`
- `src/metal_zfp/CMakeLists.txt`

### 6.2 CMake integration

At top-level:

```cmake
option(ZFP_WITH_METAL "Enable Metal GPU backend" OFF)

if(ZFP_WITH_METAL)
  if(ZFP_WITH_CUDA)
    message(FATAL_ERROR "Enable only one GPU backend: CUDA or Metal")
  endif()
  if(NOT ZFP_BIT_STREAM_WORD_SIZE EQUAL 64)
    message(FATAL_ERROR "Metal backend requires ZFP_BIT_STREAM_WORD_SIZE=64")
  endif()
  enable_language(OBJCXX)
  add_subdirectory(src/metal_zfp)
endif()
```

Ensure `.mm` sources are only compiled when Metal is enabled.

### 6.3 Runtime behavior parity

Mirror CUDA host orchestration behavior:

- same contiguity checks for host pointers,
- same failure behavior for unsupported non-contiguous host layouts,
- equivalent stream pointer/bit updates expected by `zfp` core.

### 6.4 No new public field tagging API in v1

Do not add `zfp_field_set_metal_buffer(...)` in first pass.
For v1, support host pointers robustly; add explicit GPU-buffer interop API later if needed.

---

## 7. Phase 3 - Kernel port CUDA -> MSL

**Duration:** ~2.5 weeks

Port kernels from `src/cuda_zfp` to `src/metal_zfp`.

### 7.1 Scope

- encode: 1D/2D/3D
- decode: 1D/2D/3D
- types: float, double, int32, int64
- fixed-rate mode (matching current CUDA path)

### 7.2 Mechanical mapping

- `__global__` -> `kernel`
- `__shared__` -> `threadgroup`
- `__syncthreads()` -> `threadgroup_barrier(...)`
- CUDA index arithmetic -> Metal grid/threadgroup built-ins.

### 7.3 Parameter packing

Pack scalar launch params into structs (`ZFPParams1/2/3`) in constant buffers.

### 7.4 Word/atomic handling

- Use 64-bit word type (`ulong`) to match zfp word assumption for GPU path.
- Use 64-bit atomics where available.
- If fallback is needed, guard with compile-time checks and verify correctness via dedicated overlap stress tests.

### 7.5 Kernel naming convention

Use deterministic function names encoding dim + scalar type to simplify host dispatch.

---

## 8. Phase 4 - Validation and performance tuning

**Duration:** ~2 weeks

### 8.1 Correctness matrix (must pass)

For each type `{float, double, int32, int64}` and dims `{1,2,3}`:

- contiguous arrays,
- non-multiple-of-4 sizes,
- representative small/medium/large sizes,
- strided layouts that are still contiguous in memory.

Checks:

- GPU compressed stream equals CPU reference stream where deterministic parity is expected.
- Round-trip decompression matches expected semantics:
  - ints exact,
  - floats within expected tolerance for selected mode.

### 8.2 Integration tests

Add CTest coverage for Metal backend (guarded by option):

- backend smoke tests,
- type/dim matrix,
- stride/contiguity behavior parity with CUDA/CPU path.

### 8.3 Performance tuning

Use Xcode Metal tools:

- Metal Frame Debugger,
- Metal System Trace,
- pipeline reflection (thread execution width, threadgroup memory).

Tune threadgroup sizes per kernel family; keep correctness fixed while tuning.

### 8.4 Native MSL optimization workflow (practical)

When bringing up native Metal kernels, optimize in this order:

1. **Establish a stable baseline**
   - Build with `-DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING_FULL=ON -DZFP_WITH_METAL=ON`.
   - Record baseline times from `testZfpCuda{1,2,3}d{Float,Double,Int32,Int64}` and `testzfp`.
   - Use medians over at least 5 runs per test.

2. **Kernel launch geometry first**
   - Start from `threadExecutionWidth`-aligned threadgroup sizes.
   - Sweep 1D/2D/3D kernels separately (e.g., 64/128/256 threads where valid).
   - Keep occupancy and register pressure balanced; avoid oversized threadgroups that reduce active wavefronts.

3. **Memory traffic and layout**
   - Coalesce global loads/stores for block reads and bitstream writes.
   - Stage block-local data in `threadgroup` memory when reuse is high.
   - Minimize host-device copies in the backend path; keep intermediate buffers device-resident when possible.

4. **Bit packing and atomics**
   - Prefer deterministic contiguous bit writes when possible.
   - For overlapping writes, use 64-bit atomics only where required; avoid atomics in non-overlap fast paths.
   - Validate no data races using stressed non-multiple-of-4 shapes and mixed-rate tests.

5. **Specialize by data type/dim**
   - Compile/type-specialize kernels for `{float,double,int32,int64}` and dimensions `{1,2,3}`.
   - Remove runtime branching from inner loops by selecting specialized pipelines at dispatch time.

6. **Profile, then iterate**
   - Use Metal System Trace to identify kernel vs memcpy bottlenecks.
   - Use pipeline reflection to inspect register/threadgroup usage.
   - Only keep changes that preserve correctness matrix and improve median performance.

### 8.5 Suggested perf gates for native MSL readiness

- All correctness tests in Phase 4 pass for native MSL path.
- No regression > 5% vs prior native-MSL baseline on median compress/decompress time for core 1D/2D/3D float tests.
- Demonstrated speedup over serial policy on representative medium/large 2D and 3D workloads.

### 8.6 Repeatable perf command set

Use the perf helper to collect repeated runs and summary statistics:

```bash
python3 tests/perf/run_perf_suite.py --build-dir build-metal --repeats 5
```

For targeted sweeps and CI artifacts:

```bash
python3 tests/perf/run_perf_suite.py \
  --build-dir build-metal \
  --repeats 7 \
  --suite endtoend_float="^testZfpCuda(1d|2d|3d)Float$" \
  --suite throughput="^testzfp$" \
  --json-out build-metal/perf-summary.json
```

---

## 9. Phase 5 - MLX integration (optional)

**Duration:** ~3 weeks

### 9.1 Separate optional module

Add `src/mlx_zfp/` as an optional component independent from core library correctness milestones.

### 9.2 Reuse Metal kernels

- Reuse `.metal` kernel source/binaries from Phase 3.
- Avoid code duplication between Metal backend and MLX wrapper.

### 9.3 Python bindings

- Use nanobind (aligned with MLX ecosystem patterns).
- Expose `compress` / `decompress` APIs for MLX arrays.

### 9.4 Keep blast radius small

MLX integration must not alter base C API behavior or non-MLX builds.

---

## 10. Risks and mitigations

### Risk A - Dispatch/API churn destabilizes core

Mitigation:

- keep `src/zfp.c` table shape unchanged,
- avoid new public API in first milestone,
- route through internal backend bridge.

### Risk B - 64-bit atomic portability

Mitigation:

- first-class support target = Apple Silicon,
- strict compile guards,
- dedicated correctness tests for bit-plane write races,
- document unsupported legacy hardware explicitly if needed.

### Risk C - Non-contiguous host arrays

Mitigation:

- preserve existing behavior from CUDA path,
- centralize checks in shared helper,
- test parity with current behavior.

### Risk D - bitstream word-size mismatch

Mitigation:

- configure-time guard for Metal requiring 64-bit word size,
- runtime assert retained in backend path.

### Risk E - Build portability regressions

Mitigation:

- all Metal/ObjC++ code fully option-gated,
- CI adds explicit with/without-Metal jobs on macOS,
- ensure Linux/Windows untouched when Metal disabled.

---

## 11. Proposed file layout

```text
zfp/
├── src/
│   ├── gpu_backend.h            # NEW internal backend interface
│   ├── gpu_common.h             # NEW shared host helpers
│   ├── gpu_common.cpp
│   │
│   ├── cuda_zfp/
│   │   ├── cuZFP.cu             # refactored, can be split further
│   │   ├── ... existing kernels
│   │
│   ├── metal_zfp/               # NEW
│   │   ├── metal_backend.mm
│   │   ├── metal_backend.h
│   │   ├── encode1.metal
│   │   ├── encode2.metal
│   │   ├── encode3.metal
│   │   ├── decode1.metal
│   │   ├── decode2.metal
│   │   ├── decode3.metal
│   │   └── CMakeLists.txt
│   │
│   └── mlx_zfp/                 # NEW optional
│       ├── mlx_zfp.cpp
│       ├── mlx_zfp_py.cpp
│       └── CMakeLists.txt
│
└── tests/
    └── metal/                   # NEW optional backend tests
```

---

## 12. Timeline and milestones

| Phase | Deliverable | Duration |
|---|---|---|
| 0 | Design freeze + acceptance criteria | 0.5 week |
| 1 | Internal backend bridge + shared helper extraction | 1 week |
| 2 | Metal host runtime integrated + CMake guards | 1.5 weeks |
| 3 | 6 kernels ported (encode/decode x 1/2/3D) | 2.5 weeks |
| 4 | correctness matrix + tuning + tests | 2 weeks |
| **Total (Metal)** | | **~8 weeks** |
| 5 | MLX wrapper + nanobind Python module | 3 weeks |
| **Total (Metal + MLX)** | | **~11 weeks** |

Execution order is sequential across phases, but kernel porting work in Phase 3 can be parallelized by dimension/type once scaffolding is stable.

---

## 13. Progress log (through current session)

This section tracks concrete implementation status and measured results.

### 13.1 Implemented so far

- Added internal GPU backend bridge and shared helpers:
  - `src/gpu_backend.h`
  - `src/gpu_common.h`
  - `src/gpu_common.cpp`
  - `src/cuda_zfp/cuda_backend_bridge.cpp`
- Kept `src/zfp.c` CUDA-policy dispatch shape intact while routing through backend abstraction.
- Updated template wrappers to use backend bridge:
  - `src/template/cudacompress.c`
  - `src/template/cudadecompress.c`
- Added Metal backend module and build integration:
  - `src/metal_zfp/` (runtime, backend, MSL kernels, CMake)
  - top-level and `src/` CMake changes for `ZFP_WITH_METAL` paths
- Added policy and end-to-end test integration:
  - `tests/src/execPolicy/testMetalPolicy.c`
  - CMake wiring under `tests/`
- Added perf automation helper:
  - `tests/perf/run_perf_suite.py`

### 13.2 Native Metal codec status

- Native Metal experimental codec path is active for CUDA-policy float 1D/2D/3D fixed-rate paths.
- Bitstream/checksum parity established for key tests:
  - `testZfpCuda1dFloat`
  - `testZfpCuda2dFloat`
  - `testZfpCuda3dFloat`
  - `testCuda`
  - `testMetalPolicy`
- 3D parity issue was fixed by correcting Metal 3D coefficient permutation order to match CUDA behavior (`perm_3d` parity fix in `src/metal_zfp/pack_unpack.metal`).

### 13.3 Optimization work completed

- Added non-atomic aligned bit-write mode with atomic fallback for unaligned rates.
- Added writer-side OR write combining/cache flush in block writer.
- Added interior fast paths for 2D/3D block load/store to reduce boundary-branch cost.
- Tuned dispatch threadgroup sizing in runtime.
- Added rate-specific kernel entrypoints and PSO plumbing (`r8/r16/r32`) for 1D/2D/3D float.

### 13.4 Regression and recovery notes

- The initial rate-specific pass regressed performance because it specialized only entrypoints while still invoking generic block coder internals.
- Recovery step 1:
  - reverted runtime selection back to stable generic float PSOs in `src/metal_zfp/metal_runtime.mm`.
- Recovery step 2:
  - reduced host launch overhead by reusing `zfp_metal_ctx.params_buf` for codec param structs (instead of per-launch buffer allocation).
  - adjusted codec threadgroup default from `threadExecutionWidth * 4` to `* 2`.
- Targeted validation after recovery changes passed:
  - `ctest --test-dir build-metal-native-codec -R "^testZfpCuda(1d|2d|3d)Float$|^testCuda$|^testMetalPolicy$" -V`

### 13.5 Performance snapshots (median GB/s)

Element counts used for throughput conversion:

- 1D float: `1,048,577` values
- 2D float: `1025 x 1025`
- 3D float: `129 x 129 x 129`

Median throughput summary:

| Snapshot | 1D Compress | 1D Decompress | 2D Compress | 2D Decompress | 3D Compress | 3D Decompress |
|---|---:|---:|---:|---:|---:|---:|
| `perf-native-float-rateopt.json` | 0.546 | 3.411 | 0.226 | 3.143 | 0.449 | 2.509 |
| `perf-native-float-specialized.json` | 0.343 | 0.811 | 0.074 | 0.517 | 0.281 | 0.772 |
| `perf-native-float-revert-specialized-floatonly.json` | 0.408 | 1.110 | 0.159 | 0.727 | 0.394 | 1.526 |
| `perf-native-float-paramsbuf-reuse.json` | 0.461 | 2.004 | 0.163 | 0.948 | 0.336 | 0.878 |

Notes:

- Current state has recovered from the specialized-kernel regression, but remains below the earlier best `rateopt` decode throughput, especially for 2D/3D.

### 13.6 Current performance baseline (latest measurements)

Element counts and test array sizes:

- 1D: `1,048,577` floats (~4 MB)
- 2D: `1025 x 1025` floats (~4 MB)
- 3D: `129 x 129 x 129` floats (~8.6 MB)

Metal native codec, float path (5-repeat median, seconds):

| Test | Compress | Decompress |
|------|----------|------------|
| 1D Float | 0.014 | 0.005 |
| 2D Float | 0.034 | 0.008 |
| 3D Float | 0.052 | 0.012 |

Serial CPU baseline (5-repeat median, seconds):

| Test | Compress | Decompress |
|------|----------|------------|
| 1D Float | 0.030 | 0.029 |
| 2D Float | 0.019 | 0.019 |
| 3D Float | 0.027 | 0.037 |

Speedup summary (Metal vs Serial, higher is better):

| Path | Compress | Decompress |
|------|----------|------------|
| 1D Float | **2.1x** | **5.9x** |
| 2D Float | **0.56x** (slower) | **2.4x** |
| 3D Float | **0.52x** (slower) | **3.1x** |

Key observation: Decompress is consistently faster on Metal. Compress is faster only for 1D; 2D/3D compress is ~1.8-1.9x *slower* than serial CPU.

### 13.7 Current focus

- Keep parity stable while pursuing throughput gains.
- Next high-impact item: true rate-specialized inner coder loops (real unrolled bitplane read/write paths), not only specialized kernel wrappers.

---

## 14. Metal acceleration plan

This section describes a prioritized, incremental plan to close the compress performance gap and further improve decompress throughput. Each item is ordered by expected impact and designed to be independently testable.

### 14.0 Guiding principles

1. **Correctness first**: Every optimization must pass the existing 14-test CTest suite before measuring performance. Never merge a perf change that breaks bitstream parity.
2. **Measure before and after**: Use `tests/perf/run_perf_suite.py --repeats 5` and save JSON snapshots. Compare median values.
3. **One variable at a time**: Each item below is a standalone change. Ship and measure before combining.
4. **Host overhead dominates compress**: The data shows 2D/3D compress is slower than serial. This means the GPU kernel time is *less* than the combined host overhead (buffer creation, memset, command buffer setup, synchronous wait). Host-side optimization must come first.

### 14.1 Phase A: Eliminate host-side launch overhead (highest impact)

**Problem**: Every codec call in `metal_runtime.mm` creates two new `MTLBuffer` objects via `newBufferWithBytesNoCopy`, a new `MTLCommandBuffer`, a new encoder, commits, and calls `waitUntilCompleted`. For encode, there is also a `memset(stream_words, 0, stream_bytes)` on the CPU before dispatch.

**Why this matters**: `newBufferWithBytesNoCopy` requires the Metal driver to validate page alignment and register the memory region with the GPU MMU. This takes microseconds per call -- a fixed cost that dominates when kernel execution itself is only tens of microseconds (as it is for ~4 MB arrays).

#### A1. Reuse src/dst MTLBuffer objects across calls

**Files**: `src/metal_zfp/metal_runtime.mm`

- Add `src_buf` / `dst_buf` fields to `ZfpMetalContext` for codec paths (separate from layout pack/unpack which already has them).
- Track capacity. On each codec call, check if existing buffer covers the needed byte range; only reallocate if not.
- `newBufferWithBytesNoCopy` requires page-aligned base and page-multiple length. If the caller's buffer doesn't meet these constraints, fall back to `newBufferWithLength` + memcpy (which is still cheaper than re-creating each time).
- Pattern: mirror the existing `zfp_metal_ensure_buffer()` approach already used for `params_buf`.

**Expected impact**: Removes 2 buffer creation calls per codec dispatch. Should disproportionately help 2D/3D compress where block counts (and thus the ratio of overhead to useful work) are worst.

#### A2. Move stream zero-fill to GPU

**Files**: `src/metal_zfp/metal_runtime.mm`, `src/metal_zfp/pack_unpack.metal`

- The `memset(stream_words, 0, stream_bytes)` before encode is required because the block encoder ORs bits into the output. Currently done on CPU.
- Option 1 (preferred): Add a trivial `zfp_memset_uint` MSL kernel that zeros the stream buffer on GPU. Dispatch it as the first command in the same command buffer as the encode kernel.
- Option 2: Use `MTLBlitCommandEncoder fillBuffer:range:value:` which is a Metal built-in and avoids a custom kernel.
- Either way, the zero-fill happens on the GPU timeline without a CPU stall.

**Expected impact**: Eliminates the CPU memset that touches every byte of the output buffer. For 3D float at rate 16, stream is ~2 MB -- zeroing that on CPU before GPU dispatch is pure waste.

#### A3. Batch command encoding / reduce synchronization

**Files**: `src/metal_zfp/metal_runtime.mm`

- Currently: one `MTLCommandBuffer` per kernel dispatch, committed and waited on synchronously.
- Change: for encode path, encode the zero-fill blit + codec kernel into a single command buffer, single commit.
- For decode: single command buffer, single commit (no zero-fill needed).
- Optionally: if caller does compress-then-decompress (as end-to-end tests do), investigate a "batch" API that pipelines both into one command buffer. (Lower priority; the test harness calls them separately.)

**Expected impact**: Reduces Metal driver overhead from 2 round-trips (zero + encode) to 1.

### 14.2 Phase B: Kernel-level optimization (medium impact, compress-focused)

**Problem**: The compress kernel is the bottleneck. Decompress already shows 2-6x speedup. The ZFP block encoder uses a variable-length embedded coding loop with per-bit branches -- this is inherently divergent on GPU.

#### B1. Enable rate-specialized PSO selection

**Files**: `src/metal_zfp/metal_runtime.mm` (PSO selectors), `src/metal_zfp/pack_unpack.metal` (rate-specialized kernels)

- The rate-specialized PSOs (`encode1d_float_r8_ps`, etc.) are already compiled and loaded but the `zfp_metal_select_*` functions always return the generic PSO (lines 299-339 of `metal_runtime.mm`).
- Enable selection: when `maxbits` matches 8, 16, or 32, return the rate-specialized PSO.
- The rate-specialized kernels in `pack_unpack.metal` (via `ZFP_DEFINE_RATE_KERNELS_*D` macros) pass the rate as a compile-time constant. This allows the MSL compiler to:
  - Resolve `atomic_mode = (RATE & 31u) != 0u` at compile time (rate 32 -> non-atomic, rate 8/16 -> atomic).
  - Potentially constant-fold parts of the bitplane loop upper bound.
- **Caveat from 13.4**: Previous attempt regressed because only the wrapper was specialized, not the inner coder. The current macros DO pass the rate constant into `zfp_encode_block_*d_float(fblock, RATE, ...)` so the inner coder can benefit. Re-measure carefully.

**Expected impact**: Moderate. The MSL compiler may unroll the bitplane loop and eliminate branches for known rates. Worth 10-30% on compress.

#### B2. Investigate threadgroup memory / SIMD-group cooperation

**Files**: `src/metal_zfp/pack_unpack.metal`

- Currently each thread processes one entire block independently (1 block = 4/16/64 values). There is no SIMD-group cooperation.
- ZFP's embedded coding is inherently serial *within a block* because each bitplane depends on the group-test of previous planes.
- However, the *gathering* of source data and the *lifting transform* stages are regular and could benefit from SIMD-group loads.
- Investigate:
  - Using `simd_shuffle` to broadcast/gather the 4 values in a 1D block from a SIMD lane.
  - For 3D: using threadgroup shared memory to stage the 64-float block load (coalesced read from device memory into threadgroup, then per-thread lift from threadgroup).
- This requires careful measurement. The current block sizes (4 for 1D, 16 for 2D, 64 for 3D) may not map well to SIMD widths (32 on Apple GPU).

**Expected impact**: Uncertain. 3D blocks (64 values) are the best candidate. May help 5-15% on both compress and decompress.

#### B3. Optimize the inner bitplane encoder

**Files**: `src/metal_zfp/pack_unpack.metal`

- The encode loop (lines 532-545 for 1D, 633-645 for 2D, 734-746 for 3D) has per-bit branches (`zfp_writer_write_bit`) inside a variable-iteration loop.
- The bit-writer itself (lines 416-444) has a while loop for cross-word writes.
- Potential optimizations:
  - **Pre-compute group test**: For each bitplane k, compute `x` (the OR of all bits at plane k) in a single pass, then decide the coding path. Avoid the inner branch chain when `x == 0` (early termination).
  - **Batch write for known-zero planes**: If the rate is fixed and the number of zero-planes is known, skip them without individual bit writes.
  - **Reduce write-combining cache misses**: The writer has a single-word cache (`cache_wi`, `cache_val`). For non-atomic mode, accumulate a full 64-bit word before flushing. Currently flushes on every word-index change (32-bit granularity).
- This is the most complex optimization and should come after A1-A3 to have a cleaner baseline.

**Expected impact**: 15-40% improvement on compress, especially for higher rates where more bitplanes are coded.

### 14.3 Phase C: Double/int32/int64 native kernels (expanding coverage)

**Problem**: Non-float types fall back to serial CPU. For double, this is actually *slower* than direct serial because of the backend bridge overhead (0.044-0.078s vs 0.019-0.027s for serial float of similar size).

#### C1. Double codec kernels

**Files**: `src/metal_zfp/pack_unpack.metal`, `src/metal_zfp/metal_runtime.mm`, `src/metal_zfp/metal_backend.mm`

- Port `zfp_encode_block_*d_float` / `zfp_decode_block_*d_float` to double.
- Key differences:
  - `zfp_exponent_float` -> 11-bit exponent extraction for double.
  - Quantization scale: `ldexp(1.0, 62 - emax)` instead of `ldexp(1.0f, 30 - emax)`.
  - Integer representation becomes 64-bit (`long` in MSL), requiring wider lifting transforms.
  - Bit-writer needs to handle 64-bit unsigned values in the bitplane loop.
- Add PSOs and runtime dispatch analogous to float path.
- Add dispatch cases in `metal_backend.mm` for `zfp_type_double`.

**Expected impact**: Eliminates serial fallback for double. Should achieve similar speedup ratios as float.

#### C2. Int32/int64 codec kernels

**Files**: same as C1

- Integer types skip the float-to-fixed-point conversion and exponent coding.
- The block encoder for integers: no exponent header, direct lifting transform on input values, then embedded coding.
- Simpler than float; can reuse most of the encoder infrastructure.

**Expected impact**: Lower priority since int test arrays are small (fast already). Completes coverage.

### 14.4 Phase D: Advanced / speculative optimizations (lower priority)

#### D1. Indirect command buffers for multi-field batching

- If the application compresses multiple fields in sequence, an `MTLIndirectCommandBuffer` could encode all dispatches up front and execute them with a single GPU submission.
- Requires API changes (batch-compress entry point). Out of scope for current correctness-first approach.

#### D2. Asynchronous (non-blocking) dispatch

- Currently `waitUntilCompleted` blocks the CPU. For streaming use cases, the CPU could prepare the next field while the GPU processes the current one.
- Requires signaling infrastructure (semaphores, completion handlers).
- Significant API/lifetime complexity; defer until host overhead is reduced.

#### D3. Shared memory tile loading for 2D/3D

- Load a tile of source data into threadgroup shared memory using coalesced reads, then let each thread within the group access its block from shared memory.
- Reduces device memory traffic for 2D/3D where blocks overlap in the memory layout due to padding.

### 14.5 Implementation order and milestones

| Step | Item | Files | Risk | Estimated effort |
|------|------|-------|------|-----------------|
| 1 | A1: Buffer reuse | `metal_runtime.mm` | Low | 0.5 day |
| 2 | A2: GPU zero-fill | `metal_runtime.mm`, `pack_unpack.metal` | Low | 0.5 day |
| 3 | A3: Single command buffer | `metal_runtime.mm` | Low | 0.5 day |
| 4 | Measure & snapshot | `run_perf_suite.py` | None | 0.25 day |
| 5 | B1: Rate-specialized PSOs | `metal_runtime.mm` | Medium | 0.5 day |
| 6 | B3: Inner encoder optimization | `pack_unpack.metal` | Medium | 2 days |
| 7 | Measure & snapshot | `run_perf_suite.py` | None | 0.25 day |
| 8 | C1: Double codec kernels | all three | Medium | 3 days |
| 9 | C2: Int32/int64 kernels | all three | Low | 2 days |
| 10 | B2: SIMD-group cooperation | `pack_unpack.metal` | High | 2 days |

**Total estimated effort: ~12 days**

### 14.6 Success criteria

1. **2D/3D float compress must beat serial CPU** (currently 0.52-0.56x; target >= 1.5x).
2. **Decompress should match or exceed the historical best** from `perf-native-float-rateopt.json` (1D: 3.4 GB/s, 2D: 3.1 GB/s, 3D: 2.5 GB/s).
3. **All 14 correctness tests remain passing** after every change.
4. **Double/int types run on GPU** without serial fallback (Phase C).
5. **No performance regressions** on any path vs current baseline (within measurement noise of +/- 5%).
